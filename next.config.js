/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.discordapp.com"]
    },
    trailingSlash: true,
    // reactStrictMode: true,
}

module.exports = nextConfig
